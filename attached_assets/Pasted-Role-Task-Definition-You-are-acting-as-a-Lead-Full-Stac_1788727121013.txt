Role & Task Definition:You are acting as a Lead Full-Stack Engineer and Principal UI/UX Architect. Your task is to refactor, redesign, and heavily simplify the Interaction / Engagement Page while strictly maintaining and enforcing the application's underlying visual-data synchronization workflows.1. CORE DESIGN & SIMPLIFICATION RULES (NON-NEGOTIABLE)Ultra-Minimalist Aesthetics: Eliminate visual clutter, redundant action buttons, extra popups, and non-essential wrappers. Provide a clean, modern layout with deliberate whitespace and subtle micro-interactions.Mobile-First & Fully Responsive: Ensure seamlessly adaptive layouts across mobile, tablet, and desktop viewports using clean, utility-first CSS (e.g., Tailwind CSS).Performance & Clean Architecture: Refactor component code into clean, modular TypeScript with strict type definitions and optimal reactivity to prevent unnecessary re-renders.2. MANDATORY DATA & STATE SYNCHRONIZATION (CRITICAL)You must strictly preserve and implement the following full-stack/state-binding linkages across the interactive canvas and modules:Whiteboard to Summaries Bank Integration (السبورة ↔ بنك الملخصات):Every annotation, text entry, diagram, or drawn element created on the active whiteboard during a live session MUST automatically persist and sync in real-time to the Summaries Bank (بنك الملخصات).Ensure that saved whiteboard assets are categorised, indexed, and accessible directly inside the Summaries Bank without requiring manual user export or duplicated actions.Session Progression Flow & Log Sync (جزء سير العناصر ↔ ما يدون على جزء السير):Every action, progress step, or structured note recorded within the element flow timeline (سير العناصر) MUST strictly map to and dynamically update the corresponding activity log/notes layer (ما يدون على جزء السير).The state management must guarantee two-way or transactional consistency between current timeline progress and its attached notes/logs.3. EXECUTION STEPSAudit the current Interaction Page code, identifying all components managing the Whiteboard, Element Flow Timeline, and Summaries Bank.Strip out all visual clutter and streamline the page layout to focus strictly on essential interaction pathways.Verify and refactor the state handlers (e.g., React Context, Zustand, or API state) to ensure the Whiteboard $\rightarrow$ Summaries Bank and Element Flow $\rightarrow$ Activity Log connections remain 100% active, reactive, and bug-free.Run full runtime checks to ensure no state drops occur during live edits.Proceed with the implementation immediately following these strict guidelines.